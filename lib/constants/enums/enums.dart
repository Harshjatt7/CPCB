enum UserTypes {
  admin,
  other,
  inspection,
  producer,
  retreader,
  recycler,
  custom;
}
enum SalesDataDropdown{producerType, typeOfTyre, financialYear, month}
enum UserTypeDropdown{userType}
